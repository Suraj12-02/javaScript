# Project 01 — Welcome Message Changer

**Difficulty:** Easy
**Technology:** HTML / CSS / JavaScript
**Time Target:** 5–8 minutes

## Problem Statement

Complete the `changeMessage()` function so that clicking **Start** changes the heading from:

```text
Welcome Student
```

to:

```text
Frontend Practice Started!
```

## Requirements

* Complete the `changeMessage()` function in `script.js`.
* Select the heading using its existing `id`.
* Change only the displayed text of the heading.
* Do not modify the supplied HTML.
* Keep the existing function name, event wiring, HTML IDs, and CSS unchanged.

## Expected Behaviour

### On Opening

The page should display:

```text
Welcome Student
```

### After Clicking Start

The heading should change to:

```text
Frontend Practice Started!
```

### Clicking Start Again

The same final text should remain:

```text
Frontend Practice Started!
```

## Files to Modify

Only modify the TODOs in:

```text
script.js
```

Do not modify the supplied HTML structure, IDs, function names, event handlers, or CSS.

## How to Run

No installation or server is required.

### Option 1 — Open Directly

Open `index.html` in any modern web browser.

### Option 2 — VS Code Live Server

If you have the **Live Server** extension installed in VS Code:

1. Open the project in VS Code.
2. Right-click `index.html`.
3. Select **Open with Live Server**.

## Starter Status

The starter page loads with the initial UI, but the exercise handler is intentionally incomplete.

The goal is to implement the required JavaScript behaviour.

A successful page load only confirms that the project scaffold works; it does **not** mean the exercise has been completed.

## Completion Checklist

* [ ] `changeMessage()` is completed.
* [ ] The heading is selected using its existing ID.
* [ ] Only the heading's displayed text is changed.
* [ ] Clicking **Start** changes the message correctly.
* [ ] Clicking **Start** again keeps the final message unchanged.
* [ ] No HTML or CSS changes were made.
